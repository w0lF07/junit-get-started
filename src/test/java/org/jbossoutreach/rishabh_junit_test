

import static org.junit.Assert.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;


public class Rishabh_Junit_test
{
    @Test
    public void asserttrue()
    {
        int divide = 21 % 3;         
        assertTrue(divide == 0); // If condition is false it will create an error

    }
    @Test
    public void assertfalse()
    {
        int divide = 21 % 3;
        assertFalse(divide != 0); // If condition is true it will create an error
    }
    @Test
    public void assertequals()
    {
        int divide = 21 % 3;
        int output = 0;
        assertEquals(divide, output); // If both the values are not equal it will create an error
    }
    @Test
    public void isEqual()
    {
        int a[]={1,2,3,4,5};
        int b[]={1,2,3,4,5};
        assertArrayEquals(a ,b); // If the two arrays are not equal it will create an error
    }
    public String getvalue()
    {
        String a = "Junit";
        return a;
    }
    @Test
    public void testGetTheObject() 
    {
        Rishabh_Junit_test rjn = new Rishabh_Junit_test();

        assertNotNull(rjn.getvalue()); // If the value is null it will create an error
    }
}

